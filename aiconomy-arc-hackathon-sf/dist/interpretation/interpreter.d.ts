import { IntentTemplate, InterpretedIntent } from '../core/interpretation';
/**
 * Intent Interpreter (Pure Module)
 * Translates high-level, human-readable templates into canonical InterpretedIntents.
 */
export declare function interpret(template: IntentTemplate): InterpretedIntent;
